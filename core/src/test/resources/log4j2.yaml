Configuration:
  status: warn

  appenders:
    console:
      name: console
      target: SYSTEM_OUT
      PatternLayout:
        pattern: "%d{ISO8601} %5p %c{3}: %m%n"

  loggers:
    root:
      level: info
      appender-ref:
        ref: console